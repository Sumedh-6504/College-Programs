#include <stdio.h>
#include <stdlib.h>

void push(int item);
int pop();
void palindrom();
void display();

int main(){
    int n, i;

    printf("Enter the number of stack elements: ");
    scanf("%d", &n);

    
}